# ProjectPath Environment Configuration

# Database Configuration
# For development (SQLite - default):
# DATABASE_URL=sqlite:///./projectpath.db

# For production (PostgreSQL):
# DATABASE_URL=postgresql://username:password@localhost:5432/projectpath

# Backend Settings
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend Settings
FRONTEND_HOST=localhost
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:8000/api

# API Configuration
API_TITLE=ProjectPath API
API_VERSION=1.0.0
API_DESCRIPTION=PERT/CPM Project Analyzer

# CORS Settings (development - allow all)
# For production, set specific origins:
# ALLOWED_ORIGINS=["https://yourdomain.com"]

# Logging
LOG_LEVEL=INFO

# Performance
MAX_ACTIVITIES_PER_PROJECT=10000
ANALYSIS_TIMEOUT_SECONDS=30

# Optional: Enable debug mode (development only)
# DEBUG=True
